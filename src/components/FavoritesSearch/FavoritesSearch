import React, {
  useEffect,
  useState,
} from "react";
import {
  useDispatch,
  useSelector,
} from "react-redux";

import { useHistory } from "react-router-dom";

function FavoritesSearch() {
  const dispatch = useDispatch();
  const history = useHistory();
  //   const favoritesSearch = useSelector(
  //     (store) => store.favoritesSearch
  //   );

  //fetch words on mount
  //   useEffect(() => {
  // dispatch({ type: "FETCH_FAVORITES" });
  // console.log(event.currentTarget);
  // console.log("favorites", favoritesSearch);
  //   }, []);
  return (
    <div className="container">
      <input
        type="text"
        placeholder="Favorites Search"
      ></input>
      {/* <input
        type="text"
        placeholder="Favorites Search"
        onChange={(event) => {
          dispatch({
            type: "SET_FAVORITES_SEARCH",
            payload: event.target.value,
          });
        }}
      /> */}
      {/* create a button that console logs globalSearch */}
      {/* <button
        onClick={() => {
        //   console.log("favorites:", favoritesSearch);
        }}
      >
        Log Favorites Search
      </button> */}
    </div>
  );
}

export default FavoritesSearch;
